# Shared types

This directory will hold application-wide contracts such as `PortfolioData`,
`ThemeConfig`, generated database types, and user/admin types. Their final
shapes will be defined in a later phase.
